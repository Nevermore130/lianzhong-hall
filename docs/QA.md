# 本地验收

自动化命令：`npm run build && npm test`。测试服务使用内存数据库或临时目录，不连接开发服务器。

## 已做浏览器检查

- 桌面宽屏：Windows 98 灰色工具栏、棋桌大厅、右侧房间树和玩家表格正常呈现。
- 390px 手机：棋盘与房间可操作，没有页面横向溢出。
- 单机练习：中心落子后电脑回棋，之后轮回到玩家。
- 实时房间：游客入座、准备，刷新后同一身份和准备状态保留。
- 开发 HTTP / WebSocket 代理可以完成身份启动和快照同步。

## 手动回归步骤

1. 普通窗口和无痕窗口各进入一次，两种身份显示为两位在线玩家。
2. A 创建房间，复制链接到 B；分别选择黑白席位并准备。
3. 交替落子，检查两个窗口棋盘同步；空闲第三个窗口可观战。
4. 断开 A，B 暂停落子；30 秒内恢复则继续，否则 A 负、B 胜。
5. 双方准备再开一局；旧棋谱不能继续落子，认输后只结算一次。
6. 注册/登录并完成对局，重启服务后账号、胜负和聊天仍在；进行中棋局和自建房间不恢复。
7. 搜索房名/编号、清空搜索、收藏/取消收藏、隐藏满桌、清空搜索，检查空状态。
8. 窄屏点击游戏目录、打开帮助/登录、用 Tab 和 Escape 操作弹窗。

同浏览器多个普通标签页共用 Cookie，被当作一个玩家。这是刻意设计，不是在线人数丢失。

## 账号验收（2026-09-13）

23 项自动测试通过，新增覆盖：旧库增量迁移及幂等重开、游客五胜转正保留 ID/席位/战绩、重名回滚、并发转正、密码修改/找回、令牌哈希及用途隔离/过期/重复消费、单账号并发失败锁定及跨重启保留、昵称/邮箱别名共享锁定、会话续期的 7 天/30 天边界、设备越权拒绝、撤销后关闭 WebSocket、资料广播无邮箱泄漏、注销时对局保护和匿名化结果、本地收件箱 Host 校验/转义及生产配置限制。

浏览器验收使用独立端口与临时 SQLite，未改动开发账号：

1. 游客注册，立即恢复大厅连接，工具栏出现“我的账号”。
2. 改昵称并选择青竹头像，账号窗口和在线列表同步更新。
3. 填写邮箱和当前密码，从本地收件箱打开链接，确认绑定后退出，使用邮箱重新登录。
4. 修改密码后当前浏览器保持登录，设备列表显示当前会话；其他会话撤销由真实多 WebSocket 集成测试验证。
5. 退出后点击“忘记密码”，收取本地邮件，设置新密码并重新登录。
6. 在 390×844 视口检查账号属性窗口、预设头像换行和操作按钮，页面无横向溢出。
7. 填写当前昵称、密码并勾选确认，注销测试账号后回到登录窗口；其他标签页同步退出。
8. 浏览器未发现运行错误。SMTP 仅检查适配配置，未向外部邮箱发送邮件。

本地日常验收：`npm run dev`，大厅地址 `http://127.0.0.1:5188/`，收件箱 `http://127.0.0.1:3089/`。不要在真实账号上执行注销验收。

## 聊天验收（2026-09-13）

32 项自动测试通过。聊天专项覆盖消息落盘与重启重读、50 条游标分页、跨频道隔离、成员/旁观者权限、离房撤销读取权限、写入后确认、重试幂等、限频失败后原编号重试、未读单调更新与多标签同步、注销清理、刷新待发恢复、历史合并与断线跨度处理。

浏览器使用独立数据库和测试助手，未向开发大厅发送测试消息：

1. 预置 125 条大厅历史，首屏读取最近 50 条；点击加载更早消息后有 100 条，滚动位置保持在原内容处。
2. 阅读旧消息时收到新消息，滚动位置不变，出现“1 条未读消息”，点击后回到底部并清除徽标。
3. 连续发送触发限频，第二条显示失败、保留原文；点击重试后只有一条已发送消息。
4. 进入第一桌自动切换房间频道；切回大厅后收到房间消息，只显示房间未读数，不混入大厅内容。
5. 失败消息在刷新后恢复原文与频道，重试成功且无重复。
6. 390×844 视口中聊天频道、消息区与输入区无横向溢出。

7. 打开设置弹窗时收消息，保留未读徽标；关闭弹窗、消息区可见后清除。
8. 离开房间后通过“待发”窗口看到原房间和原文，重试被禁用，可复制或移除；不会误发到大厅。
9. 窗口在桌面与手机尺寸间切换，处于底部时仍显示最新消息；浏览器未发现运行错误。

同账号多标签页已读同步、断线后原编号去重由自动化真实 WebSocket 测试覆盖。

## 五子棋素材验收（2026-09-13）

- Chrome 桌面和 390×844 响应式预览：木纹、四边坐标、黑白棋子与末手标记正常显示。
- 单机练习点击 H8，电脑执白应手；桌面 A1、O15 边角落子正确，已占用位置不可再次落子。
- 联机房间复用新棋盘与席位棋子；旁观状态棋盤不可落子。
- 三张 WebP 与 SVG 标记均返回 HTTP 200；构建、32 项现有测试和格式检查通过。

## 斗地主验收（2026-09-13）

42 项自动测试通过。新增测试使用独立服务和临时数据库，未改动开发账号战绩：

- 14 种牌型、点数边界、非法翅膀、重复牌、炸弹/王炸比较、两家不出后的重新领出。
- 叫分加叫、3 分立即确定地主、全员不叫重新发牌；叫分中最高叫分人尚不公开为地主，底牌仍隐藏。
- 20 组固定种子电脑完整对局，无丢牌/重复牌，结算分数和为零；普通胜负、春天和反春天。
- 三个真实 WebSocket 客户端完成一局，另一个客户端旁观；逐人手牌隔离、旁观操作拒绝、越权持牌和旧 revision 指令拒绝。
- 同账号新连接恢复本人手牌，农民断线期间暂停出牌、超时全队判负；叫分阶段退出取消且不公开底牌、不计战绩。
- 三人结果和胜负幂等落库；数据库重启后保留结果与默认桌频道 ID；注销匿名化参与者并保留其余玩家战绩。

浏览器检查：

1. 斗地主目录展示六张圆桌和三席人数；选桌、第三席入座、准备、站起、返回大厅可用，房间聊天自动切换。
2. 单机游戏出现两位电脑，3 分叫地主后手牌从 17 张变为 20 张，三张底牌公开；电脑轮流应手。
3. 提示可选中顺子并出牌；手动选择、取消和重选正常，不能压过上一手时禁用出牌，没有合法接牌时显示“不出”提示。
4. 重新发牌清空上一局并返回叫分阶段，17 张手牌和隐藏底牌恢复。
5. 桌面与 390×844 视口均能操作；房间和练习没有页面横向溢出，已显示素材全部加载成功，浏览器无运行错误日志。
6. 快速进桌遵循当前游戏筛选，房间及侧栏选中状态与斗地主保持一致；进桌后可自行选择席位。

进一步人工验收可使用三个独立浏览器身份入座并准备，另开第四身份观战，对比手牌与底牌可见性。计时、联机托管和服务重启续局尚未实现；开发热更新重启后出现身份请求中断时，可通过原游客入口重试恢复，不会创建新身份。

## 扑克牌质感第二版（2026-09-13）

- 新增七张 imagegen PNG 母版与 WebP 运行图：旧纸底纹、做旧牌背、红黑大小王、J/Q/K 双头插画。最终小王已检查真实 alpha，弃用带方格背景的试稿。
- 54 张精确 SVG 角标/花色与共享位图分层合成，原有牌 ID 与玩法协议一致；页面中的每张牌仍有独立无障碍名称。
- 浏览器整副牌预览：54 张牌齐全，104 个图像元素全部加载，未发现控制台错误；放大牌面与 20 张手牌尺寸下的角标和花色可辨，页面无横向溢出。
- 游戏房间与单机练习使用新版组件，纸面、牌背和 J/Q/K 在真实牌桌中呈现；本次未操作开发账号的联机对局或战绩。
- 构建、42 项既有回归测试、格式检查与差异检查通过。新资源清单的 63 个文件全部存在。

可打开 `/assets/playing-cards-v2/preview.html` 单独比较大小王、J/Q/K、牌背与全部点数；原图和最终提示词在 `assets/source/playing-cards-v2/PROMPTS.md`。

### 人物显示比例修正

- 修复 J/Q/K 的 2:3 原图被拉伸到约 40:84 的问题：改用等比缩放，并加宽内框、调整角标间距。
- 大小王按可见图案包围盒归一高度和中心，消除透明留白不同造成的大小与偏移差异；源位图保持不变。
- 浏览器检查 160px 放大牌面、62px 叠放手牌和 80px 完整牌组：五种人物的实际宽高比均约为 0.6667，与源图一致；54 张牌齐全，全部图像加载成功，无页面横向溢出。
- 本次构建、涉及文件的格式检查与差异检查通过；变更仅涉及牌面合成与生成脚本，未重复运行游戏规则测试。

## 中国象棋验收（2026-09-14）

62 项自动测试通过，其中象棋新增 20 项：

- 32 子标准开局、44 个先手合法走法，不可变走棋与错误回合/位置拒绝。
- 車直线阻挡、炮架、蹩马腿、塞象眼、相象不过河、九宫和兵卒过河；不能自将或将帅照面。
- 将死、困毙判负、三次重复局面、单方长将判负、连续 120 步未吃子和棋；吃子清零计数。
- 电脑找到一步将死，连续电脑走棋保持将帅安全。
- 三个真实 WebSocket 身份完成走棋、旁观、房间聊天、认输、再来一局与将死；拒绝旁观走棋、跨游戏指令、旧版本与越权操作。
- 求和只能由对手回应，旧请求无效，走棋撤销请求；和棋记录落库但不增加胜负。
- 断线暂停、恢复原局面与席位、超时仅结算一次；象棋结果与默认房间编号在数据库重开后保留。

浏览器验收：

1. 中国象棋筛选展示六张棋桌，桌面叠加象棋棋盘，保留原座椅和人物层次。
2. 进入房间自动选择房间聊天；旁观时 90 个交点均不可操作；执红入座、单人准备等待与站起正常。
3. 练习 E4 兵仅提示 E5；走兵后电脑应手并记录吃子，悔棋恢复 32 子标准开局。
4. 选择执黑后黑方位于下方，电脑执红先走；提示 A10 → B10 后实际移动黑車吃炮，翻转坐标映射正确。
5. 390×844 断点下棋盘与棋谱纵向排列，棋子保持正圆，操作区可见，页面无横向溢出。恢复默认视口后继续操作正常。
6. 联机棋盘按窗口高度缩放，对局中的聊天区保留 104px；1280×720 中可同时看完整棋盘的两端。
7. 棋盘与棋子图层全部加载成功，浏览器没有警告或运行错误。开发账号仍为 1 胜 0 负，联机测试使用隔离数据库。

构建、格式检查通过。素材 PNG 母版与生成提示词在 `assets/source/xiangqi-v1/`，WebP、SVG 字层及清单在 `public/assets/xiangqi-v1/`。当前采用页面帮助声明的娱乐重复局面规则；复杂长捉裁定、比赛计时与服务重启续局未实现。

## 手机布局验收（2026-09-14）

本次使用浏览器 CSS 视口模拟，检查 402×874 竖屏、874×402 横屏、402×740 浏览器工具栏占用高度，以及 402×480 输入时的短视口；这不等同于 iPhone 真机 Safari 验收。

- 大厅：竖屏工具栏按两行展示，游戏切换与房间列表入口可见，两列棋桌独立滚动；横屏工具栏恢复一行，聊天放到棋桌右侧。所检视口无页面横向溢出。
- 游戏：象棋练习选兵、合法落点、电脑应手及展开棋谱正常；五子棋落点与棋盘交点对齐；斗地主可叫 3 分、滑动到手牌末端、选牌与出牌。58×84.1px 手牌保持原始宽高比，不修改素材文件。
- 联机房间：进入空象棋桌后工具栏自动收起；402×874 下棋盘约 280×311px，完整棋盘、44px 棋谱开关与底部操作均可见。更短视口保留游戏区滚动。离开后恢复大厅工具栏。
- 聊天（按后续反馈更新）：聚焦输入框保持原位，不隐藏工具栏、棋桌或状态栏；使用 16px 字号、30px 高输入框与发送按钮。402×480 下两者底部均在 449px，仍在可见范围内。未发送验收消息。
- 注册：输入框为 16px/44px，窄屏无横向裁切；短视口下内容可滚动，关闭按钮保留在顶部。未注册或修改开发账号。
- 安全区：启用 `viewport-fit=cover`，大厅及弹窗考虑四边 `safe-area-inset-*`。高度监听 `visualViewport`，随键盘和浏览器工具栏变化；捏合缩放时保留浏览器原生缩放行为。实现依据 [WebKit 安全区说明](https://webkit.org/blog/7929/designing-websites-for-iphone-x/) 与 [VisualViewport 文档](https://developer.mozilla.org/en-US/docs/Web/API/VisualViewport)。刘海、系统键盘动画和实际触摸手势仍需真机复核。

62 项现有测试、TypeScript/Vite 构建与格式检查通过。验收后恢复默认桌面视口；桌面仍使用原有工具栏、房间/玩家侧栏与棋谱布局。

### 手机控件密度调整

- 工具栏按钮由 52px 改为 40px，图标与文字横排；切换按钮约 32px，普通操作约 36px，入座、准备和叫分/出牌保留 40px。
- 表单与搜索输入框从 44px 改为 36px，保留 16px 文字；聊天输入框随后进一步缩至 30px，发送按钮与其等高，缩短占位文字。注册表单压缩字段间距，弹窗从约 479px 缩短至 424px。
- 402×874 下大厅棋桌区从 391px 增至 467px；聊天区从 160px 调整为 136px。空象棋房间棋盘约 330×367px，棋谱入口和底部按钮均完整显示。
- 874×402 横屏检查斗地主：叫分按钮为 40px，手牌独立滑动，页面无横向溢出。聊天移除全屏输入模式和“完成”按钮，聚焦时维持原布局；Escape 或点击其他控件可结束输入。
- 本次仅调整样式与输入提示文案，构建、格式与差异检查通过，未重复运行游戏规则测试。浏览器验收后恢复桌面视口，未发送测试聊天或修改账号。

聊天原位输入复核：402×874 下聚焦前后，聊天区均为 136px，棋桌区均为 467px；402×480 下工具栏、棋桌与状态栏保持显示。删除了专门驱动全屏模式的 React 状态与样式；保留 `visualViewport` 对键盘可见高度的适配。
